package handler

import "github.com/gin-gonic/gin"

func Register(c *gin.Context) {
    // TODO: 解析请求 -> 调用 service.Register -> 返回统一响应
}

func Login(c *gin.Context) {
    // TODO: 解析请求 -> 调用 service.Login -> 返回 token
}
